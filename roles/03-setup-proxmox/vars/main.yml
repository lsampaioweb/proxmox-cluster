---
# The password that Ansible will use to connect and run commands into each host.
ansible_become_password: "{{ lookup('pipe', 'security find-generic-password -a $USER -s {{ ansible_password_id }} -w') }}"

smtp_server: "smtp.gmail.com"
smtp_port: "587"
smtp_email: "lsampaioweb@gmail.com"
smtp_password: "{{ lookup('pipe', 'security find-generic-password -a $USER -s {{ proxmox_smtp_password_id }} -w') }}"
